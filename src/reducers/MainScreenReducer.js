/**
 * Created by mponomarets on 6/25/17.
 */
import {

} from '../actions/types';

const INITIAL_STATE = {
	title: 'Some title'
};

export default (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state;
	}
};